# batch-MP4-SRT-to-MKV
Converts all sets of MP4s and SRTs in a folder into MKVs. Keeps all audio and subtitle tracks.

# Prerequisites
To run this script, ffmpeg is required. This can be installed on Windows from command prompt using
```
winget install ffmpeg
```
Python3 is also required.
